
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Redirecting...</title>
  <style>
    #loading {
      position: fixed;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      width: 100%;
      height: 100%;
      top: 0;
      left: 0;
      z-index: 9999; /* make sure it's on top of other elements */
    }

    #loading-image {
      width: 300px; /* Adjust the size as needed */
      height: 300px; /* Adjust the size as needed */
    }

    #welcome-heading {
      margin-top: 20px;
      font-size: 48px;
      color: #262626; /* Adjust color as needed */
      animation: fadeInOut 3s infinite alternate; /* Define animation */
    }

    /* Define fadeInOut animation */
    @keyframes fadeInOut {
      0% {
        opacity: 0;
        font-family: 'Arial', sans-serif;
      }
      25% {
        opacity: 1;
        font-family: 'Courier New', monospace;
      }
      50% {
        opacity: 1;
        font-family: 'Times New Roman', serif;
      }
      75% {
        opacity: 1;
        font-family: 'Georgia', serif;
      }
      100% {
        opacity: 1;
        font-family: 'Verdana', sans-serif;
      }
    }

    /* Media query for mobile devices */
    @media only screen and (max-width: 500px) {
      #loading-image {
        width: 150px; /* Adjust the size for smaller screens */
        height: 150px; /* Adjust the size for smaller screens */
      }

      #welcome-heading {
        font-size: 24px; /* Adjust font size for smaller screens */
      }
    }

    @import url("https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600");

:root {
  --bar-scale-y: 0;
  --sparkle-color: rgb(253 244 215 / 40%);
}

@keyframes pop-word {
  to {
    transform: rotateX(0);
  }
}

@keyframes show {
  to {
    opacity: 1;
  }
}

@keyframes bar-scale {
  to {
    transform: scaleY(1);
  }
}

@keyframes sparkle {
  0% {
    transform: scale(0);
  }

  60% {
    transform: scale(1) translate(4px, 1px) rotate(8deg);
  }

  100% {
    transform: scale(0) translate(4px, 1px) rotate(8deg);
  }
}

@keyframes shimmer {
  to {
    text-shadow: 0 0 8px red;
  }
}

body {
  display: grid;
  height: 100vh;

  background-color: rgb(255, 254, 254);
  place-items: center;
}

h1 {
  color: rgb(26, 65, 81);
  font-family: "Playfair Display", Vidaloka, serif;
  font-size: 8rem; /* Initial font size */
  line-height: 0.85;
  perspective: 500px;
}

/* Increase the size of letters to 85% */
.word {
  display: block;
  font-size: 85%; /* Increase font size to 85% */
  animation: show 0.01s forwards, pop-word 1.5s forwards;
  animation-timing-function: cubic-bezier(0.14, 1.23, 0.33, 1.16);
  opacity: 0;
  transform: rotateX(120deg);
  transform-origin: 50% 100%;
   /* Increase bottom margin */
}


.word:nth-of-type(2) {
  padding: 0 1rem; /* Adjust padding */
  animation-delay: .5s;
  color: rgb(32, 32, 31);
}

.superscript {
  position: relative;
  animation-delay: 2s;
  animation-duration: 0.25s;
  animation-name: shimmer;
  vertical-align: text-top;
 
}

/* bars */
.superscript::before {
  --bar-width: 25%;
  position: absolute;
  top: 37%;
  left: 47%;
  width: 14%;
  height: 48%;
  animation: bar-scale 0.25s linear 3s 1 forwards;
  background: linear-gradient(
    to right,
    rgb(28, 29, 30) var(--bar-width),
    transparent var(--bar-width) calc(100% - var(--bar-width)),
    rgb(28, 29, 29) calc(100% - var(--bar-width))
  );
  content: "";
  transform: scaleY(var(--bar-scale-y));
}

/* Media query for screens with max-width 600px */
@media screen and (max-width: 600px) {
  h1 {
    font-size: 5rem; /* Adjust font size */
    text-align: center; /* Center align the text */
    justify-content: center; /* Center align horizontally */
    align-items: flex-start; /* Align items at the top */
  }

  /* sparkle */
  .superscript::after {
    --size: 3rem; /* Reduce sparkle size */
  }

  /* Adjust font size for smaller screens */
  .word {
    font-size: 80%; /* Increase font size to 85% */
  }
}

  </style>
  <script>
    function redirectToLogin() {
    // Get the device width
    var deviceWidth = window.innerWidth || document.documentElement.clientWidth || document.body.clientWidth;
    
    // Check if the device width is less than 500 pixels
    if (deviceWidth < 500) {
        window.location.href = '/index'; // Redirect to '/index' for small screens
    } else {
        window.location.href = '/Index1'; // Redirect to '/index1' for larger screens
    }
}

      

    // Call the redirectToLogin function after a delay of 1 second
    setTimeout(redirectToLogin, 3500);
  </script>
</head>
<body>
  <div id="loading">
    <h1>
      <span class="word">MITE<span class="superscript"></span></span>
      <span class="word"> Food Court</span>
      
    </h1>
    <img id="loading-image" src="../Images/nutrition-plan.gif" alt="Loading..." />
    <h2 id="welcome-heading">Welcome..</h2>
  </div>
</body>
</html>

